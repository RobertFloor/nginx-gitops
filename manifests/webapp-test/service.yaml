apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: nginx-test
  labels:
    app: webapp
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: webapp
